@using g212102053_mehmet_kaan_akcan_odev.Models.Siniflar
@model List<History>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/AdminLayout.cshtml";
}

<h2>Anasayfa Bilgileri</h2>
<table class="table table-bordered" style="margin-top:20px;">
    <tr>
        <th>Tarihçe</th>
        @if (User.IsInRole("A"))
        {
            <th>İşlemler</th>
        }
        </tr>
    @foreach (var m in Model)
    {
        <tr>
            <td>
                @m.HistoryDescription
            </td>
            @if (User.IsInRole("A"))
            {
                <td>
                    <a href="/AdminHistory/HistoryUpdate/@m.HistoryID" class="btn btn-success">Güncelle</a>
                </td>
            }
            </tr>
    }
</table>



